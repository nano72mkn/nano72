---
import BaseLayout from '../layouts/BaseLayout.astro';
import Logo from '../components/Logo.astro';
---

<BaseLayout title="会社情報" description="nano72の会社情報をご紹介します。">
  <section class="page-header">
    <div class="container">
      <h1>会社情報</h1>
    </div>
  </section>

  <section class="content">
    <div class="container">
      <div class="company-info">
        <div class="info-section">
          <Logo size={100} className="company-logo" />
          <h2>nano72について</h2>
          <p>
            nano72（ナノナツ）は、日常生活を少しだけ豊かにするスマートフォンアプリケーションの開発を行っています。<br>
            「小さな太陽」のような温かさを持つアプリケーションを通じて、ユーザーの皆様の毎日に小さな幸せをお届けすることを目指しています。
          </p>
        </div>

        <div class="info-section">
          <h2>会社概要</h2>
          <dl class="info-list">
            <div class="info-item">
              <dt>名称</dt>
              <dd>nano72（ナノナツ）</dd>
            </div>
            <div class="info-item">
              <dt>事業形態</dt>
              <dd>個人事業</dd>
            </div>
            <div class="info-item">
              <dt>事業内容</dt>
              <dd>
                <ul>
                  <li>スマートフォン向けアプリケーションの企画・開発・運営</li>
                  <li>Webサービスの開発</li>
                </ul>
              </dd>
            </div>
            <div class="info-item">
              <dt>ドメイン</dt>
              <dd>nano72.com</dd>
            </div>
          </dl>
        </div>

        <div class="info-section">
          <h2>開発理念</h2>
          <div class="philosophy">
            <div class="philosophy-item">
              <h3>シンプルであること</h3>
              <p>複雑な機能よりも、本当に必要な機能を分かりやすく提供します。</p>
            </div>
            <div class="philosophy-item">
              <h3>温かみがあること</h3>
              <p>技術的な正しさだけでなく、使う人の心に寄り添うアプリを作ります。</p>
            </div>
            <div class="philosophy-item">
              <h3>継続的に改善すること</h3>
              <p>ユーザーの声を大切にし、より良いアプリへと進化させ続けます。</p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .page-header {
    background: linear-gradient(135deg, #fff8f0 0%, #fff 100%);
    padding: calc(var(--spacing-unit) * 8) 0 calc(var(--spacing-unit) * 6);
    text-align: center;
  }

  .page-header h1 {
    color: var(--color-primary);
    font-size: 2.5rem;
    margin: 0;
  }

  .content {
    padding: calc(var(--spacing-unit) * 8) 0;
  }

  .company-info {
    max-width: 800px;
    margin: 0 auto;
  }

  .info-section {
    margin-bottom: calc(var(--spacing-unit) * 8);
  }

  .info-section:last-child {
    margin-bottom: 0;
  }

  .company-logo {
    margin: 0 auto calc(var(--spacing-unit) * 4);
  }

  .info-section h2 {
    text-align: center;
    color: var(--color-text);
    margin-bottom: calc(var(--spacing-unit) * 4);
  }

  .info-section > p {
    text-align: center;
    line-height: 1.8;
    color: var(--color-text);
    max-width: 600px;
    margin: 0 auto;
  }

  .info-list {
    background: var(--color-gray-light);
    border-radius: 12px;
    padding: calc(var(--spacing-unit) * 4);
  }

  .info-item {
    display: flex;
    padding: calc(var(--spacing-unit) * 2) 0;
    border-bottom: 1px solid var(--color-gray);
  }

  .info-item:last-child {
    border-bottom: none;
    padding-bottom: 0;
  }

  .info-item:first-child {
    padding-top: 0;
  }

  .info-item dt {
    font-weight: 700;
    color: var(--color-text);
    min-width: 120px;
    flex-shrink: 0;
  }

  .info-item dd {
    color: var(--color-text);
    margin: 0;
    flex: 1;
  }

  .info-item ul {
    list-style: none;
    margin: 0;
  }

  .info-item ul li {
    position: relative;
    padding-left: calc(var(--spacing-unit) * 2);
    margin-bottom: calc(var(--spacing-unit) * 0.5);
  }

  .info-item ul li:last-child {
    margin-bottom: 0;
  }

  .info-item ul li::before {
    content: '•';
    position: absolute;
    left: 0;
    color: var(--color-primary);
  }

  .philosophy {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: calc(var(--spacing-unit) * 4);
  }

  .philosophy-item {
    text-align: center;
    padding: calc(var(--spacing-unit) * 3);
    background: var(--color-gray-light);
    border-radius: 12px;
  }

  .philosophy-item h3 {
    color: var(--color-primary);
    font-size: 1.25rem;
    margin-bottom: calc(var(--spacing-unit) * 2);
  }

  .philosophy-item p {
    color: var(--color-text);
    line-height: 1.6;
    margin: 0;
  }

  @media (max-width: 768px) {
    .page-header {
      padding: calc(var(--spacing-unit) * 6) 0 calc(var(--spacing-unit) * 4);
    }

    .page-header h1 {
      font-size: 2rem;
    }

    .content {
      padding: calc(var(--spacing-unit) * 6) 0;
    }

    .info-item {
      flex-direction: column;
    }

    .info-item dt {
      margin-bottom: calc(var(--spacing-unit) * 1);
    }

    .philosophy {
      grid-template-columns: 1fr;
    }
  }
</style>